[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "archiver-bot"
version = "0.1.0"
dependencies = [
  "matrix-nio",
  "aiohttp",
]

[project.scripts]
archiver-bot = "archiver_bot:main"
